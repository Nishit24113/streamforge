[project]
name = "streamforge"
version = "1.0.0"
description = "Serverless real-time data pipeline platform"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-v --tb=short"
